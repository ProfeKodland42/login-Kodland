# URL del backend (Flask en Render) para PRODUCCIÓN.
# En local NO hace falta: Vite hace proxy a http://127.0.0.1:5000 (ver vite.config.js).
# En Vercel, define esta variable en Settings > Environment Variables con la URL de Render, ej:
# VITE_API_URL=https://kodland-backend.onrender.com
VITE_API_URL=
